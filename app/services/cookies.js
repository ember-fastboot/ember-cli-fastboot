import Ember from 'ember';

export default Ember.Service.extend({
  // TODO: implement cookies access as wrapper around either request/response (as set by ember-fastboot-server) if in fastboot mode
  // or document.cookie if in browser mode
});
